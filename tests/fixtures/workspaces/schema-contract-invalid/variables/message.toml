schema_version = 1
schema = "../schemas/value.schema.json"
description = "Variable whose referenced schema cannot compile"

[values]
default = { enabled = "not a boolean" }

[env._]
value = "default"
