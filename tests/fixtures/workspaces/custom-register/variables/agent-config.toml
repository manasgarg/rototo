schema_version = 1

description = "Agent config with a registered custom value lint"
schema = "../schemas/config.schema.json"

[values.standard]
max_output_tokens = 6000

[values.small]
max_output_tokens = 1000

[env._]
value = "standard"
