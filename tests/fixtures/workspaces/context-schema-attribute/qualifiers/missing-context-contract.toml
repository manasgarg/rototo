schema_version = 1

[qualifier]
description = "References a context path not declared in the workspace context schema"

[[qualifier.predicate]]
attribute = "account.plan"
op = "eq"
value = "enterprise"
