schema_version = 1
type = "string"

[resolve]
default = "off"

# `env.bogus` is not a real env member (only env.qualifier[...] and env.now are).
# Expect rototo/variable-rule-invalid-reference.
[[resolve.rule]]
when = "env.bogus"
value = "on"
