schema_version = 1
type = "string"

[resolve]
default = "off"

# No list named missing_tiers is declared under lists/.
# Expect rototo/expression-unknown-list.
[[resolve.rule]]
when = '"free" in lists.missing_tiers'
value = "on"
