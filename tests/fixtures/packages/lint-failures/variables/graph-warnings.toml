schema_version = 1

description = "Variable with graph warnings"
type = "string"

[resolve]
default = "control"

[[resolve.rule]]
when = 'qualifier["self-cycle"]'
value = "first"

[[resolve.rule]]
when = 'qualifier["self-cycle"]'
value = "duplicate"
