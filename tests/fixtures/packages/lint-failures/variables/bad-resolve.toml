schema_version = 1

description = "Variable with bad variable and value references"
type = "catalog:bad-resolve"

[resolve]
default = "control"

[[resolve.rule]]
when = 'variables["missing-condition"]'
value = "another-missing-value"
