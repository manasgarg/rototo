schema_version = 1

description = "Variable with bad qualifier and value references"
type = "catalog:bad-resolve"

[resolve]
default = "control"

[[resolve.rule]]
when = 'env.qualifier["missing-qualifier"]'
value = "another-missing-value"
