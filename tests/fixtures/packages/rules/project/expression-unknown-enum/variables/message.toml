schema_version = 1
description = "Greeting selected by plan tier membership."
type = "string"

[resolve]
default = "hello"

[[resolve.rule]]
when = 'context.tier in enums.plan_tiers'
value = "welcome"
