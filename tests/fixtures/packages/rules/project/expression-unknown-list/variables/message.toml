schema_version = 1
description = "Greeting selected by plan tier membership."
type = "string"

[resolve]
default = "hello"

[[resolve.rule]]
when = 'context.tier in lists.plan_tiers'
value = "welcome"
