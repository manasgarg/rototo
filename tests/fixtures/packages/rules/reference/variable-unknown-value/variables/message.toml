schema_version = 1
description = "Configuration value for message."
type = "catalog=message"

[resolve]
default = "missing"

[[resolve.rule]]
when = 'context.user.tier == "premium"'
value = "control"
