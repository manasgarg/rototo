[package]
name = "rototo-go"
version = "0.1.0-alpha.8"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rototo = { path = "../..", default-features = false }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "sync", "time"] }
