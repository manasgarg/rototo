[package]
name = "rototo-go"
version = "0.1.0-alpha.4"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rototo = { path = "../.." }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "sync", "time"] }
