{
  "$comment": "The visitor facts a tenant's site supplies per request. Audience conditions read these; rototo never stores visitor lists.",
  "type": "object",
  "title": "Site visitor context",
  "properties": {
    "visitor": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "visits": { "type": "integer", "minimum": 0 },
        "lifetime_spend": { "type": "number", "minimum": 0 }
      },
      "required": ["id"]
    }
  },
  "required": ["visitor"]
}
