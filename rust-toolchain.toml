# Single source of truth for the Rust toolchain across local dev and CI.
# Track stable for now; pin an exact version later if release reproducibility
# matters more than automatically picking up stable compiler improvements.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy", "rust-analyzer"]
