{
    "name": "@rototo/console-server",
    "version": "0.1.0-alpha.6",
    "description": "rototo console server: TypeScript spine over the Rust core bindings.",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "build:native": "napi build --platform --release",
        "build:native:debug": "napi build --platform",
        "typecheck": "tsc -p tsconfig.json",
        "test": "node --test \"tests/*.test.ts\"",
        "check": "npm run typecheck && npm test",
        "dev": "node src/main.ts",
        "format": "prettier --write \"package*.json\" \"tsconfig.json\" \"src/**/*.ts\" \"tests/**/*.ts\"",
        "format:check": "prettier --check \"package*.json\" \"tsconfig.json\" \"src/**/*.ts\" \"tests/**/*.ts\""
    },
    "napi": {
        "binaryName": "rototo-console-native",
        "targets": [
            "x86_64-unknown-linux-gnu",
            "aarch64-unknown-linux-gnu",
            "x86_64-apple-darwin",
            "aarch64-apple-darwin"
        ]
    },
    "dependencies": {
        "@hono/node-server": "^1.19.7",
        "hono": "^4.10.7"
    },
    "devDependencies": {
        "@napi-rs/cli": "3.7.0",
        "@types/node": "24.10.2",
        "prettier": "^3.8.4",
        "typescript": "6.0.3"
    }
}
