schema_version = 1

description = "Per-request observability policy for rototo console"
type = "catalog:console-request-observability"

[resolve]
default = "standard"

[[resolve.rule]]
when = 'qualifier["request-dev-console"]'
value = "dev-all"

[[resolve.rule]]
when = 'qualifier["request-server-error"]'
value = "retain-errors"

[[resolve.rule]]
when = 'qualifier["request-slow"]'
value = "retain-slow"
