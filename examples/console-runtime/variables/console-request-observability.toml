schema_version = 1

description = "Per-request observability policy for rototo console"
type = "catalog:console-request-observability"

[resolve]
default = "standard"

[[resolve.rule]]
when = 'variables["request-dev-console"]'
value = "dev-all"

[[resolve.rule]]
when = 'variables["request-server-error"]'
value = "retain-errors"

[[resolve.rule]]
when = 'variables["request-slow"]'
value = "retain-slow"
