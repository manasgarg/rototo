schema_version = 1
description = "Console startup using the dev host"
type = "bool"

[resolve]
default = false

[[resolve.rule]]
when = '(context.phase == "startup") && (context.console.host == "dev.rototo.dev")'
value = true
