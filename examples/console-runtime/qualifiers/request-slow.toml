schema_version = 1

description = "Request latency exceeded the baseline observability threshold"

[[predicate]]
attribute = "phase"
op = "eq"
value = "request"

[[predicate]]
attribute = "response.latency_ms"
op = "gt"
value = 750
