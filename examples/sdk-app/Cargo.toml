[package]
name = "rototo-sdk-app"
version = "0.1.0-alpha.3"
edition = "2024"
publish = false

[dependencies]
rototo = { path = "../.." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
