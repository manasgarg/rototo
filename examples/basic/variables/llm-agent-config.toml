schema_version = 1

description = "LLM settings for the main agent loop"
type = "catalog:llm-agent-config"

[resolve]
default = "standard"

[[resolve.rule]]
when = 'qualifier["enterprise-accounts"]'
value = "enterprise"
