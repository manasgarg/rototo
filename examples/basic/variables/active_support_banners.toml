schema_version = 1

description = "Every enabled support banner, for surfaces that list all of them"
type = "list<catalog=support_banner>"

[resolve]
method = "query"
from = "support_banner"
filter = "entry.enabled == true"
sort = "entry.heading"
