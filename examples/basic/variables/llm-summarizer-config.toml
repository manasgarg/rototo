schema_version = 1

description = "LLM settings for background summarization"
type = "catalog:llm-summarizer-config"

[resolve]
default = "fast"

[[resolve.rule]]
when = 'env.qualifier["internal-staff"]'
value = "rich"
