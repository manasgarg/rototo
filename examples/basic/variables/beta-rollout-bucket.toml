schema_version = 1
description = "Stable ten percent rollout bucket for experiments"
type = "bool"

[resolve]
default = false

[[resolve.rule]]
when = '(bucket(context.user.id, "checkout-redesign-2026-05", 0, 1000))'
value = true
