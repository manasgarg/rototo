schema_version = 1
description = "Enterprise accounts operating in the European region"
type = "bool"

[resolve]
default = false

[[resolve.rule]]
when = '(variables["enterprise-accounts"]) && (context.request.region == "eu")'
value = true
