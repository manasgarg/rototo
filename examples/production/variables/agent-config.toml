schema_version = 1

description = "LLM agent profile loaded by production services"
type = "catalog:agent-config"

[resolve]
default = "standard"

[[resolve.rule]]
when = 'variables["eu-enterprise-accounts"]'
value = "enterprise"
