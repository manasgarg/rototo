schema_version = 1

description = "LLM agent profile loaded by production services"
schema = "../schemas/agent-config.schema.json"

[env._]
value = "standard"

[env.dev]
value = "local"

[env.prod]
value = "standard"

[[env.prod.rule]]
description = "EU enterprise accounts use the larger production profile"
qualifier = "eu-enterprise-accounts"
value = "enterprise"
