schema_version = 1

description = "LLM agent profile loaded by production services"
type = "resource:agent-config"

[resolve]
default = "standard"

[[resolve.rule]]
qualifier = "eu-enterprise-accounts"
value = "enterprise"
