{
  "$comment": "The facts supplied when sending a message on behalf of a customer account. account.id is the unit the migration layer hashes.",
  "type": "object",
  "title": "Message send context",
  "properties": {
    "account": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "jurisdiction": { "type": "string", "x-rototo-ref": "list=jurisdictions" },
        "plan_tier": { "type": "string", "x-rototo-ref": "list=plan_tiers" }
      },
      "required": ["id", "jurisdiction", "plan_tier"]
    },
    "message": {
      "type": "object",
      "properties": {
        "channel": { "type": "string", "x-rototo-ref": "list=channels" }
      },
      "required": ["channel"]
    }
  },
  "required": ["account", "message"]
}
